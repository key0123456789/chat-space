.message
  .message__upper-info
    .message__upper-info__talker
      = message.user.name
    .message__upper-info__date 
      = message.created_at.strftime("%Y/%m/%d(%a) %H:%M")
  %P.message__text 
    = message.content